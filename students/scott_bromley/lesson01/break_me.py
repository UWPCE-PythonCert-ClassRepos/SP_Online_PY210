#!/usr/local/bin/python3

# Create four functions, one for each exception: NameError, TypeError, SyntaxError, AttributeError


def name_error(aList=[]):
    if someList is not None:
        print(someList)
    return someList

name_error(aList=[])

def type_error(someList=[]):
    return someList % 2

type_error(someList=[])

def syntax_error():
    retrn None

syntax_error()

def attribute_error(someList=[]) :
    someList.delete(1)
    return None

attribute_error()

